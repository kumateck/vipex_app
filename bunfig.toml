[serve.static]
plugins = ["bun-plugin-tailwind"]
env = "BUN_PUBLIC_*"

[test]
# Load .env.test and tweak globals before any test files run
preload = ["./tests/setup.ts"]
timeout = 15000
# Enable coverage if you want a summary after tests
coverage = true
