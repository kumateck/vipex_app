#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running pre-push checks (typecheck + tests)..."
bun run typecheck || exit 1
bun test || exit 1
# Optionally also build to catch build-time issues:
# bun run build || exit 1