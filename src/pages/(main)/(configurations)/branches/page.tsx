const Branches = () => {
  return (
    <div>
      {/* <DataGrid
        data={data}
        columns={columns}
        onDataChange={setData}
        onDeleteRow={handleDeleteRow}
        enableSelection={true}
        enableEditing={true}
        getRowId={(row) => row.id}
      /> */}
    </div>
  );
};

export default Branches;
